<%= form_for(@residency) do |f| %>
  <% if @residency.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@residency.errors.count, "error") %> prohibited this residency from being saved:</h2>

      <ul>
      <% @residency.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
